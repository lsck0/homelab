# Terraform
.terraform/
**/.terraform/
*terraform.tfvars
*.tfvars
.tf-last-apply

# Nix
result
result-*

# key material and certificates stay local
secrets/

# OS
.DS_Store
.env
.direnv/

# Stack Secrets
letsencrypt/
auth/htpasswd

# Test/build artifacts
images/
*.iso
*.img
*.qcow2
*.sock
*.tfvars.json

# rollback copy written by src/scripts/renumber.sh
*.tfstate.pre-renumber*
# terraform's own state copies: the previous state on every apply, plus one
# per `state mv` during a renumber. Regenerated locally, never worth a commit.
terraform.tfstate.backup
*.tfstate.*.backup

# the pasted task list; scratch, not part of the repo
errors.txt
__pycache__/
*.pyc
